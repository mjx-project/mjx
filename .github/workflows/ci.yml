name: ci

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Build mjx
      run: make build -j 4

    - name: Test mjx
      run: make test

