name: ci

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Cache external
      uses: actions/cache@v2
      with:
        path: |
          external
        key: external-${{ hashFiles('cmake/*') }}

    - name: Build mjx
      run: make build -j 4 USE_SYSTEM_BOOST=OFF USE_SYSTEM_GRPC=OFF

    - name: Test mjx
      run: make test

