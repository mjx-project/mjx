name: py

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Install mjx CLI
      run: cd pymjx && make install

    - name: Run pytest
      run: cd pymjx && make pytest

    - name: Run checks
      run: cd pymjx && make checks

    - name: Test CLI (mjlog => mjproto, mjproto => mjlog)
      run: cd pymjx && make test-cli
