name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: sotetsuk/ubuntu-gcc-grpc:latest
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: |
        make build
    - name: Run tests
      run: |
        make test
