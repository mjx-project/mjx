name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: |
        ./build.sh
    - name: Run tests
      run: |
        ./build/test/mahjong_test
    env:
      CXX: /usr/bin/clang++
